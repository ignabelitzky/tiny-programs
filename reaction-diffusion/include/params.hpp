#ifndef PARAMS_HPP
#define PARAMS_HPP

namespace params {
    const int width = 1600;
    const int height = 900;
    const float dA = 1.0f;
    const float dB = 0.5f;
    const float feed = 0.055f;
    const float kill = 0.062f;
}

#endif