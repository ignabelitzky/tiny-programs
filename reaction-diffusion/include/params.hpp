#ifndef PARAMS_HPP
#define PARAMS_HPP

namespace params {
    const int width = 800;
    const int height = 600;
    const float dA = 1.0f;
    const float dB = 0.5f;
    const float feed = 0.055f;
    const float kill = 0.062f;
}

#endif